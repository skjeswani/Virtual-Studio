import React from 'react';

// This component is no longer used as the theme toggle has been removed
// and the application is now permanently in dark mode.
const ThemeToggle: React.FC = () => {
    return null;
};

export default ThemeToggle;
