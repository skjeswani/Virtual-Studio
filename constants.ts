import type { Product } from './types';

// The hard-coded product catalog has been removed.
// The app now uses a dynamic catalog stored in the user's browser (localStorage).
export const PRODUCT_CATALOG: Product[] = [];
